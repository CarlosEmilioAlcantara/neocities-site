import { paginateNext } from "../../utils/pagination/paginateNext";
import { 
  restorePositionDimension
} from "../../utils/viewport/restorePositionDimension";

const handleClickNext = (component, offset, window) => {

}